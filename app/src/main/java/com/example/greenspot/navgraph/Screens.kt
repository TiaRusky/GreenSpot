package com.example.greenspot.navgraph

enum class GreenspotScreen(){
    SignIn,
    SpotterProfile
}

const val ROOT = "root"         //Identify the root navgraph (Main graph)
const val WELCOME = "welcome"   //Welcome route of the app (Login and Sign up screens)

